import "./chunk-EQCVQC35.js";

// node_modules/.pnpm/@swup+astro@1.4.1_@types+babel__core@7.20.5/node_modules/@swup/astro/dist/idle.js
function onIdle(callback, { timeoutFallback = 1e3 } = {}) {
  if ("requestIdleCallback" in window) {
    window.requestIdleCallback(() => callback());
  } else {
    setTimeout(() => callback(), timeoutFallback);
  }
}
function onWindowLoad(callback) {
  if (document.readyState === "complete") {
    setTimeout(() => callback(), 0);
  } else {
    window.addEventListener("load", () => callback());
  }
}
function onIdleAfterLoad(callback, { delayAfterLoad = 0 } = {}) {
  onWindowLoad(() => {
    if (delayAfterLoad > 0) {
      setTimeout(() => onIdle(callback), delayAfterLoad);
    } else {
      onIdle(callback);
    }
  });
}
export {
  onIdle,
  onIdleAfterLoad,
  onWindowLoad
};
//# sourceMappingURL=@swup_astro_idle.js.map
