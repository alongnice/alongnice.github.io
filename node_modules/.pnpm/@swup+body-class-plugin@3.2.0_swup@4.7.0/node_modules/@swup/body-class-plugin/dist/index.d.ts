import { Handler } from 'swup';
import Plugin from '@swup/plugin';
type Options = {
    /** If set, only classes beginning with this string will be added/removed. */
    prefix: string;
};
export default class SwupBodyClassPlugin extends Plugin {
    name: string;
    requires: {
        swup: string;
    };
    defaults: Options;
    options: Options;
    constructor(options?: Partial<Options>);
    mount(): void;
    protected updateBodyClass: Handler<'content:replace'>;
    protected updateClassNames(el: HTMLElement, newEl: HTMLElement): void;
    protected isValidClassName(className: string): boolean | "";
}
export {};
